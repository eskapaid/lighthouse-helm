{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lighthouse-beacon-node.fullname" . }}-config
data:
  lighthouse-beacon-node.yaml: |
{{ toYaml .Values.config | indent 4 }}
{{- end }}