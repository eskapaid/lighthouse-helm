apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "lighthouse-validator.fullname" . }}
  labels:
    {{- include "lighthouse-validator.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "lighthouse-validator.selectorLabels" . | nindent 6 }}
  serviceName: {{ include "lighthouse-validator.fullname" . }}  
  replicas: {{ .Values.replicas }}
  template:
    metadata:
      labels:
        {{- include "lighthouse-validator.selectorLabels" . | nindent 8 }}
    spec:
      initContainers:
        - name: setup
          image: busybox
          command:
          - /bin/sh
          - -c
          - mkdir -p /data/medalla/validators && cp -rL /data/mount/* /data/medalla/validators/
          volumeMounts:
          - name: validator-data
            mountPath: /data
          {{- range .Values.secretMounts }}
          - name: {{ .name }}
            mountPath: {{ .path }}
          {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          {{- if .Values.container.command }}
          command:
{{ toYaml .Values.container.command | indent 12 }}
          {{- end }}
          {{- if .Values.container.args }}
          args:
{{ toYaml .Values.container.args | indent 12 }}
          {{- end }}
          resources: 
{{ toYaml .Values.resources | indent 12 }}
          volumeMounts:
            - name: validator-data
              mountPath: /data
          ports:
            - name: metrics
              containerPort: {{ .Values.ports.metrics }}
      volumes:
        {{- range .Values.secretMounts }}
          - name: {{ .name }}
            secret:
              secretName: {{ .secretName }}
        {{- end }}
  volumeClaimTemplates:
    - metadata:
        name: validator-data
      spec:
        accessModes:
          - "ReadWriteOnce"
        resources:
          requests:
            storage: {{ .Values.storage.capacity | quote }}